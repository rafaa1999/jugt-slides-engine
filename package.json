{
  "name": "jugt-presentation-system",
  "version": "1.0.0",
  "private": false,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "concurrently -k -n GEN,VITE -c blue,green \"node scripts/watch.mjs\" \"vite\"",
    "build": "node scripts/generate.mjs --prod && vite build",
    "preview": "vite preview",
    "lint:node": "node -c scripts/generate.mjs && node -c scripts/watch.mjs"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "chokidar": "^4.0.3",
    "vite": "^6.0.0"
  }
}